#include "Vector.h"
#include <stdio.h>

/*
int main()
{
	struct vector *v = vector_new();

	for (int i = 0; i < 10000; i++)
	{
		vector_push(v, i);
		int pop;
		vector_pop(v, &pop);
		printf("%i  cap=%i \n", pop, (int) v -> capacity);
	}

	printf("\n");
	print_vector(v);	


	return 0;
}
*/
